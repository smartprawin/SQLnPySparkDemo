# Project Instructions for Abacus AI

// This file is automatically loaded into Abacus AI's system prompt for every chat turn in this
// workspace. Use it to give the agent project-specific context, conventions, and rules.
//
// Examples of what to put here:
// - Tech stack (e.g. "This is a PHP 8 + SQLite project, no Composer/npm on the PHP side")
// - Coding conventions (e.g. "Use tabs not spaces", "Every database query must be a prepared statement")
// - Architecture notes (e.g. "admin/ reads techgolpo.com's DB directly via filesystem SQLite, not an API")
// - What to avoid (e.g. "Do not introduce new npm dependencies", "Do not modify the vendor/ folder")
// - Testing instructions (e.g. "Run npm test before considering a task done")

// Delete this template content and write your own instructions below.

